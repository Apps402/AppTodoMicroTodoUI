apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: todoui-ingress
spec:
  ingressClassName: azure-application-gateway
  rules:
    - host: ui.aparnasapps.icu
      http:
        paths:
        -  pathType: Prefix
           path: "/"
           backend:
             service:
               name: apps-todoui-svc
               port:
                 number: 80
